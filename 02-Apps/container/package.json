{
  "name": "terraform-sample",
  "version": "0.0.1",
  "description": "A sample Node.js app we can put in a Docker image",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "start": "node dist/src/app.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.744.0",
    "@aws-sdk/client-secrets-manager": "^3.744.0",
    "@aws-sdk/lib-dynamodb": "^3.744.0",
    "aws-lambda": "^1.0.7",
    "express": "^4.19.2",
    "express-promise-router": "^4.1.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.142",
    "@types/express": "^4.17.21",
    "@types/morgan": "^1.9.9",
    "@types/node": "^22.0.0",
    "aws-sdk-client-mock": "^4.0.1",
    "dotenv": "^16.4.5",
    "rimraf": "^6.0.1",
    "typescript": "^5.5.4"
  }
}
